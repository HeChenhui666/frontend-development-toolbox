# 前端开发工具箱 Chrome 扩展

一个功能强大的Chrome浏览器扩展，集成了多种实用的前端开发工具，包括二维码生成/解码、URL参数编辑、时间戳转换、JSON工具（解析/比对/Schema生成/TypeScript转换/CSV转换）、颜色工具集（转换器/配色建议/渐变生成）、图片工具（随机图片/Base64编码）、CSS预设工具（代码片段/兼容性检测）、正则表达式测试等，帮助开发者提高工作效率。

## 功能特性

### 🔲 二维码工具

- **生成二维码**：自动获取当前标签页URL或手动输入内容，生成二维码
- **解码二维码**：上传二维码图片，自动识别并解码内容
- **下载保存**：生成的二维码可以下载保存
- **复制功能**：支持复制URL和解码结果
- **快速打开**：解码的URL可以直接在新标签页打开

### 🔗 URL参数编辑

- **自动解析**：自动获取并解析当前标签页的URL参数
- **可视化编辑**：以表格形式展示和编辑URL参数
- **实时预览**：实时显示编辑后的完整URL
- **快速操作**：支持添加、删除、修改参数，一键复制完整URL
- **预设参数管理**：支持自定义预设参数组合，可添加、编辑、删除预设，自动缓存保存
- **预设快速应用**：一键应用预设参数组合到当前URL

### ⏰ 时间戳转换

- **实时显示**：实时显示当前时间和时间戳（秒/毫秒）
- **双向转换**：支持时间戳转日期时间，日期时间转时间戳
- **多种格式**：支持秒级和毫秒级时间戳
- **格式化显示**：标准化的日期时间格式（YYYY-MM-DD HH:mm:ss）

### 📄 JSON工具

- **JSON解析**：格式化、验证、压缩、转义/去转义JSON字符串
- **JSON比对**：对比两个JSON对象，高亮显示差异
- **JSON Schema生成器**：根据JSON数据自动生成JSON Schema规范
- **TypeScript转换**：将JSON数据转换为TypeScript接口定义
- **JSON转CSV**：将JSON数组转换为CSV格式，支持自定义分隔符
- **语法高亮**：美观的JSON展示格式
- **错误提示**：详细的错误信息提示

### 🎨 颜色工具集

- **颜色转换器**：支持HEX、RGB、RGBA、HSL、HSLA格式互转，实时同步更新
- **颜色搭配建议器**：基于色轮理论生成配色方案，支持互补色、三元色、类似色、分裂互补色、四色配色、单色系
- **渐变背景生成器**：
  - 支持线性渐变和径向渐变两种类型
  - 支持添加多个颜色停止点
  - 线性渐变支持角度调整
  - HSL颜色选择器，支持透明度
  - 实时预览渐变效果
  - 自动生成CSS渐变代码，一键复制

### 🔤 正则表达式测试

- **实时匹配**：输入正则表达式和测试文本，实时显示匹配结果
- **高亮显示**：匹配的文本会高亮显示
- **标志位设置**：支持设置全局(g)、忽略大小写(i)、多行(m)等标志位
- **预设正则**：内置常用正则表达式（手机号、身份证、邮箱、URL等）
- **错误提示**：正则表达式语法错误时显示详细提示

### 🖼️ 图片工具

- **随机图片生成器**：
  - 自定义尺寸：输入宽度和高度（1-5000像素）生成随机图片
  - 格式选择：支持JPG和WebP格式
  - 防缓存机制：自动添加时间戳和随机UID参数防止浏览器缓存
  - 图片预览：实时预览生成的图片
  - 便捷操作：支持复制URL、下载图片、在新标签页打开
  - 快速刷新：一键刷新生成新的随机图片
- **Base64编码/解码**：
  - 文本编码/解码：支持文本的Base64编码和解码
  - 图片转换：支持图片与Base64字符串互转
  - 图片预览：转换后的图片可以实时预览
  - 一键复制：快速复制Base64字符串

### ⚙️ 设置界面

- **统一设置入口**：点击标题栏设置按钮打开设置界面
- **分类管理**：设置界面包含多个设置分类（通用、主题等）
- **通用设置**：
  - **标签页排序**：自定义功能标签页的显示顺序，支持拖拽排序
  - **默认功能标签页**：设置打开扩展时默认显示的功能
  - **数据管理**：
    - 查看存储使用情况（已使用/总容量）
    - 查看详细存储信息：显示每种缓存类型的占用空间
    - 选择性清除缓存：分别清除主题、预设、游戏积分、用户偏好
    - 一键清除所有缓存
  - **关于信息**：显示版本号、GitHub仓库链接、问题反馈链接、使用文档链接
- **主题设置**：内置多种精美主题，包括红橙暖色（默认）、明亮、赛博、黑暗、梦幻粉紫等
- **一键切换**：在设置界面的主题分类中快速切换主题
- **自动缓存**：选择的主题会自动保存，下次打开时自动应用
- **全面适配**：所有功能模块和交互元素都完美适配主题色彩
- **Ant Design组件**：使用 Ant Design 组件库，提供统一的UI体验

## 安装步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 构建项目

```bash
npm run build
```

### 3. 加载扩展

1. 打开Chrome浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角的"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择项目的 `dist` 目录

## 开发

### 开发模式（自动构建）

```bash
npm run dev
```

### 生产构建

```bash
npm run build
```

## 使用说明

### 🔲 二维码工具

**生成二维码：**

1. 点击扩展图标打开弹窗
2. 选择"二维码"标签，默认进入"生成"子标签
3. 系统会自动填充当前标签页的URL，也可以手动输入任意内容
4. 点击"生成二维码"按钮
5. 生成的二维码可以下载保存或复制

**解码二维码：**

1. 在"二维码"标签中选择"解码"子标签
2. 点击"选择二维码图片"按钮
3. 选择包含二维码的图片文件
4. 系统会自动识别并显示解码结果
5. 可以复制解码结果，如果是URL可以直接打开

### 🔗 URL参数编辑

1. 点击"URL参数"标签
2. 系统会自动获取并解析当前标签页的URL参数
3. 在参数列表中可以直接编辑参数的键和值
4. 点击"添加参数"可以添加新的参数
5. 点击参数行的删除按钮可以删除参数
6. 编辑后的完整URL会实时显示在顶部
7. 点击"复制URL"可以复制完整的URL

**预设参数管理：**

1. 点击预设参数列表旁的"管理"按钮
2. 在弹窗中可以查看所有预设参数
3. 点击"添加预设"可以创建新的预设参数组合
4. 点击预设项的"编辑"按钮可以修改预设名称和参数
5. 点击"删除"按钮可以删除不需要的预设
6. 点击"重置为默认"可以恢复默认预设列表
7. 所有自定义预设会自动保存，下次打开时自动加载

### ⏰ 时间戳转换

1. 点击"时间戳"标签
2. 顶部实时显示当前时间和对应的时间戳（秒/毫秒）
3. **时间戳转日期**：输入时间戳（秒或毫秒），点击"转换"按钮
4. **日期转时间戳**：选择日期和时间，点击"转换"按钮
5. 转换结果会显示在对应的输出框中

### 📄 JSON工具

**JSON解析：**

1. 点击"JSON"标签，在工具选择器中选择"JSON解析"
2. 输入或粘贴JSON字符串
3. 选择处理模式：格式化、压缩、转义或去转义
4. 点击"校验并处理"按钮
5. 如果JSON格式错误，会显示详细的错误信息

**JSON比对：**

1. 在"JSON"标签中选择"JSON比对"
2. 在左右两个输入框中分别输入要对比的JSON
3. 系统会自动对比并高亮显示差异
4. 相同的部分显示为绿色，不同的部分显示为红色

**JSON Schema生成：**

1. 在"JSON"标签中选择"Schema生成"
2. 输入JSON数据（对象或数组）
3. 系统会自动生成对应的JSON Schema规范
4. 可以复制生成的Schema代码

**TypeScript转换：**

1. 在"JSON"标签中选择"TypeScript"
2. 输入JSON数据
3. 系统会自动生成TypeScript接口定义
4. 支持嵌套对象和数组的自动类型推断
5. 可以复制生成的TypeScript代码

**JSON转CSV：**

1. 在"JSON"标签中选择"转CSV"
2. 输入JSON数组数据
3. 选择CSV分隔符（逗号、分号、制表符）
4. 系统会自动转换为CSV格式
5. 可以复制或下载CSV文件

### 🎨 颜色工具集

**颜色转换器：**

1. 点击"颜色工具"标签，选择"颜色转换器"
2. 输入颜色值（支持HEX、RGB、RGBA、HSL、HSLA格式）
3. 系统会自动识别格式并转换为其他所有格式
4. 可以实时查看所有格式的颜色值
5. 支持颜色选择器可视化选择颜色

**颜色搭配建议器：**

1. 在"颜色工具"标签中选择"颜色搭配"
2. 选择或输入基础颜色
3. 选择配色方案类型（互补色、三元色、类似色等）
4. 点击"生成配色"按钮
5. 系统会生成对应的配色方案，显示颜色值和预览
6. 可以复制单个颜色值或整个配色方案

**渐变背景生成器：**

1. 在"颜色工具"标签中选择"渐变背景"
2. 选择渐变类型（线性/径向）
3. 对于线性渐变，可以调整角度
4. 点击颜色停止点可以编辑颜色
5. 使用HSL颜色选择器调整颜色和透明度
6. 可以添加或删除颜色停止点
7. 实时预览渐变效果
8. 点击"复制CSS"按钮复制生成的CSS代码

### 🔤 正则表达式测试

1. 点击"正则"标签
2. 输入正则表达式模式，或从预设中选择常用正则
3. 输入要测试的文本
4. 选择正则标志位（g、i、m等）
5. 系统会实时显示匹配结果
6. 匹配的文本会高亮显示
7. 如果正则表达式有语法错误，会显示错误提示

### 🖼️ 图片工具

**随机图片生成器：**

1. 点击"图片工具"标签，选择"随机图片"
2. 输入图片宽度和高度（1-5000像素）
3. 选择图片格式（默认JPG或WebP）
4. 点击"生成随机图片"按钮
5. 系统会自动生成图片URL并显示预览
6. 可以复制URL、下载图片或在新标签页打开
7. 点击刷新按钮可以生成新的随机图片
8. 图片URL会自动添加时间戳和随机UID参数防止浏览器缓存

**Base64编码/解码：**

1. 在"图片工具"标签中选择"Base64编码"
2. **文本模式**：
   - 输入要编码的文本或Base64字符串
   - 点击"编码"或"解码"按钮
   - 结果会显示在输出框中，可以一键复制
3. **图片模式**：
   - 点击"选择图片"按钮上传图片文件
   - 系统会自动转换为Base64字符串
   - 可以预览转换后的图片
   - 可以复制Base64字符串或下载转换后的图片

### 🎨 CSS预设工具

**CSS代码片段：**

1. 点击"CSS预设"标签，选择"CSS代码片段"
2. 在顶部选择分类（布局相关、视觉效果、响应式设计等）
3. 在左侧列表中选择具体的代码片段
4. 右侧会显示代码片段详情：
   - 代码内容（带语法高亮）
   - 兼容性说明
   - 示例效果预览
5. 点击"复制代码"按钮可以快速复制代码片段

**CSS兼容性检测：**

1. 在"CSS预设"标签中选择"兼容性检测"
2. 输入要检测的CSS属性（如：`backdrop-filter`、`grid`等）
3. 点击"检测"按钮
4. 系统会显示：
   - 当前浏览器是否支持
   - 各主流浏览器的支持情况
   - 兼容性说明和建议

### ⚙️ 设置界面

**打开设置：**

1. 点击标题栏右上角的设置按钮（⚙️图标）
2. 在设置界面中可以看到左侧的侧边栏导航，包含不同的设置分类

**通用设置：**

1. 点击"通用"分类可以查看通用设置选项
2. **标签页排序**：
   - 点击"管理排序"按钮打开排序管理弹窗
   - 在弹窗中拖拽列表项调整功能标签页的显示顺序
   - 点击"保存"按钮保存排序，刷新页面后生效
   - 点击"重置为默认"可以恢复默认排序
3. **默认功能标签页**：
   - 在下拉框中选择打开扩展时默认显示的功能
   - 选择后立即保存并生效
4. **数据管理**：
   - 查看存储使用情况：显示已使用空间和总容量，以及使用进度条
   - 查看详细存储信息：点击"查看详情"展开显示每种缓存类型的占用空间
   - 选择性清除缓存：在详情列表中点击对应类型的"清除"按钮，可以分别清除：
     - 主题设置
     - URL预设参数
     - 游戏积分
     - 用户偏好（默认标签页和标签页排序）
   - 清除所有缓存：点击"清除所有缓存"按钮一键清除所有数据
5. **关于信息**：
   - 查看当前版本号
   - 访问GitHub仓库、问题反馈、使用文档等链接

**主题设置：**

1. 点击"主题"分类可以进入主题设置
2. 在主题设置中可以看到所有可用主题的预览
3. 每个主题都有预览效果，显示渐变和表面颜色
4. 点击主题卡片即可切换主题
5. 选择的主题会自动保存，下次打开时自动应用
6. 所有功能模块的界面元素都会根据主题自动调整颜色

## 技术栈

- **React 18** - UI框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Ant Design 6** - UI组件库
- **QRCode.js** - 二维码生成库
- **jsQR** - 二维码解码库
- **Chrome Extension API** - 浏览器扩展API

## 项目结构

```text
hch-frontend-development-toolbox/
├── src/
│   ├── App.tsx                 # 主应用组件
│   ├── App.css                 # 主样式文件
│   ├── main.tsx                # 入口文件
│   └── components/             # 功能组件目录
│       ├── QRCodeGenerator/    # 二维码生成器
│       ├── QRCodeDecoder/      # 二维码解码器
│       ├── URLParamsEditor/    # URL参数编辑器
│       ├── TimestampConverter/ # 时间戳转换器
│       ├── JSONTools/          # JSON工具
│       │   ├── JSONParser/     # JSON解析器
│       │   ├── JSONCompare/    # JSON比对工具
│       │   ├── JSONSchemaGenerator/ # JSON Schema生成器
│       │   ├── JSONToTypeScript/ # TypeScript转换器
│       │   └── JSONToCSV/      # JSON转CSV工具
│       ├── ColorTools/         # 颜色工具集
│       │   ├── ColorConverter/ # 颜色转换器
│       │   ├── ColorPalette/  # 颜色搭配建议器
│       │   └── GradientGenerator/ # 渐变背景生成器
│       ├── RegexTester/        # 正则表达式测试器
│       ├── ImageTools/         # 图片工具
│       │   ├── RandomImageGenerator/ # 随机图片生成器
│       │   └── Base64Encoder/ # Base64编码/解码工具
│       ├── CSSTools/          # CSS预设工具
│       │   ├── CSSSnippets/   # CSS代码片段集合
│       │   └── CSSCompatibility/ # CSS兼容性检测
│       ├── Settings/           # 设置界面组件
│       ├── ThemeSettings/      # 主题设置组件
│       └── EasterEgg/          # 彩蛋页面（游戏）
│   └── utils/                  # 工具函数目录
│       ├── theme.ts            # 主题管理工具
│       ├── presetParams.ts     # 预设参数管理工具
│       ├── gameScore.ts        # 游戏积分管理工具
│       └── userPreferences.ts  # 用户偏好设置管理工具
├── icons/                      # 扩展图标
├── dist/                       # 构建输出目录
├── scripts/                    # 构建脚本
├── manifest.json               # Chrome扩展清单文件
├── package.json                # 项目配置
└── vite.config.ts              # Vite配置
```

## 添加新功能

项目采用模块化设计，使用懒加载机制，可以轻松添加新功能：

### 步骤

1. **创建功能组件**
   在 `src/components/` 目录下创建新的功能组件目录，例如 `NewFeature/`，包含：
   - `index.tsx` - 组件主文件
   - `index.css` - 组件样式文件

2. **在 App.tsx 中添加配置**

   ```typescript
   // 1. 导入组件（使用懒加载）
   const NewFeature = lazy(() => import('./components/NewFeature'));
   
   // 2. 更新 FeatureTab 类型
   type FeatureTab = 'qrcode' | 'urlparams' | 'timestamp' | 'gradient' | 'json' | 'regex' | 'newfeature';
   
   // 3. 在 features 数组中添加配置
   {
     id: 'newfeature',
     name: '新功能',
     icon: '✨',
     component: <NewFeature />,
   }
   ```

3. **重新构建**

   ```bash
   npm run build
   ```

### 组件开发建议

- 使用 TypeScript 编写，确保类型安全
- 组件应该是独立的，不依赖其他功能模块
- 使用 CSS 模块或独立的 CSS 文件管理样式
- 遵循项目的代码风格和结构
- 如果功能包含子功能，可以参考 `QRCodeGenerator` 或 `JSONTools` 的实现方式

## 注意事项

### 通用注意事项

- 扩展需要访问当前标签页的权限（`activeTab`）以获取URL信息
- 每个功能模块都是独立的，互不影响
- 所有数据都在本地处理，不会上传到服务器

### 功能特定注意事项

**二维码工具：**

- 确保二维码图片清晰，否则可能无法正确识别
- 支持的图片格式：JPG, PNG, GIF等常见图片格式
- 生成的二维码大小可以根据内容自动调整

**URL参数编辑：**

- 不支持 `chrome://`、`chrome-extension://` 和 `about:` 协议的页面
- 参数键值对会自动进行URL编码/解码
- 预设参数支持自定义名称和多个参数组合
- 预设参数数据保存在浏览器本地存储中

**时间戳转换：**

- 时间戳支持秒级（10位）和毫秒级（13位）两种格式
- 日期时间格式为：YYYY-MM-DD HH:mm:ss

**JSON工具：**

- JSON解析支持标准的JSON格式，支持格式化、压缩、转义/去转义
- JSON比对会进行深度比较，包括嵌套对象和数组
- JSON Schema生成器支持自动推断类型和生成规范
- TypeScript转换支持嵌套对象和数组的自动类型推断
- JSON转CSV支持自定义分隔符和表头处理

**颜色工具集：**

- 颜色转换器支持HEX、RGB、RGBA、HSL、HSLA格式互转
- 颜色搭配建议器基于色轮理论，支持6种配色方案
- 渐变背景生成器生成的CSS代码可以直接在项目中使用
- 支持最多10个颜色停止点

**正则表达式测试：**

- 支持JavaScript标准正则表达式语法
- 预设正则表达式可以根据需要修改

**图片工具：**

- 随机图片生成器使用 [Lorem Picsum](https://picsum.photos/) 服务生成随机图片
- 图片URL格式：`https://picsum.photos/宽度/高度?t=时间戳&uid=随机ID`
- 支持1-5000像素的图片尺寸
- 自动添加防缓存参数确保每次生成不同的图片
- Base64编码/解码支持文本和图片两种模式
- 图片转换支持常见图片格式（JPG、PNG、GIF、WebP等）
- Base64字符串可以直接用于CSS的`background-image`或HTML的`<img>`标签

**CSS预设工具：**

- CSS代码片段集合包含9大分类，共60+个实用代码片段
- 每个代码片段都包含浏览器兼容性说明和降级方案
- 代码片段提供实时预览效果
- CSS兼容性检测基于Can I Use数据库，显示准确的浏览器支持情况

**设置界面：**

- 设置界面采用侧边栏导航设计，方便管理多个设置分类
- 所有设置数据保存在浏览器本地存储中
- 切换主题后所有界面元素会立即更新
- 标签页排序保存后需要刷新页面才能生效
- 清除主题或用户偏好设置后会自动刷新页面
- 设置界面支持扩展，可以轻松添加新的设置分类和选项
- 使用 Ant Design 组件库，提供统一的UI体验和主题色适配

## 常见问题

### Q: 为什么无法获取当前页面的URL？

A: 请确保扩展已正确安装并拥有 `activeTab` 权限。某些特殊页面（如 `chrome://` 页面）无法获取URL是正常的。

### Q: 二维码解码失败怎么办？

A: 请确保：

- 图片清晰，二维码没有被遮挡
- 图片格式正确（支持 JPG、PNG、GIF 等）
- 二维码在图片中占据足够大的比例

### Q: 开发模式下修改代码后需要重新加载扩展吗？

A: 使用 `npm run dev` 启动开发模式后，代码会自动重新构建。你需要在 Chrome 扩展管理页面点击扩展的刷新按钮来加载最新版本。

### Q: 如何打包发布到 Chrome 应用商店？

A:

1. 运行 `npm run build` 构建生产版本
2. 在 Chrome 扩展管理页面点击"打包扩展程序"
3. 选择 `dist` 目录作为扩展根目录
4. 按照 Chrome Web Store 的发布流程提交

## 更新日志

### v1.6.0

- 🎨 **CSS预设工具**：
  - CSS代码片段集合：9大分类，60+个实用代码片段
  - CSS兼容性检测：检测CSS属性在当前浏览器的支持情况
  - 实时预览：代码片段提供示例效果预览
  - 一键复制：快速复制代码片段
- 🖼️ **图片工具增强**：
  - Base64编码/解码：支持文本和图片的Base64转换
  - 图片预览：转换后的图片可以实时预览
- 🎨 **颜色工具集**：
  - 颜色转换器：HEX/RGB/HSL/RGBA格式互转
  - 颜色搭配建议器：基于色轮理论的配色方案生成
  - 渐变背景生成器：集成到颜色工具集中
- 📄 **JSON工具增强**：
  - JSON Schema生成器：自动生成JSON Schema规范
  - TypeScript转换：将JSON转换为TypeScript接口定义
  - JSON转CSV：支持自定义分隔符
  - 工具选择器：使用Ant Design Select下拉框替代标签切换

### v1.5.1

- 🔧 数据管理增强：选择性清除缓存功能
- 📊 详细存储信息：显示每种缓存类型的占用空间
- 🎨 UI组件升级：全面使用 Ant Design 组件库
- 🎯 主题色适配：所有按钮、输入框、下拉框等组件完美适配主题色

### v1.5.0

- ⚙️ 通用设置功能：
  - 标签页排序：支持拖拽自定义功能标签页顺序
  - 默认功能标签页：设置打开扩展时的默认显示功能
  - 数据管理：查看存储使用情况，支持清除缓存
  - 关于信息：显示版本号和相关链接

### v1.4.0

- ⚙️ 统一设置界面，包含多个设置分类
- 🎨 主题设置集成到设置界面中
- 🔗 URL参数编辑器支持预设参数管理（添加、编辑、删除、缓存）
- 🎯 优化主题系统，调整默认主题为红橙暖色

### v1.3.0

- 🎨 主题设置功能，支持多种精美主题切换
- 🎯 优化主题适配范围，确保所有功能模块完美适配

### v1.2.0

- 🎮 彩蛋页面游戏功能（俄罗斯方块、贪吃蛇、2048）
- 📊 游戏积分缓存机制，记录历史最高分
- 🎨 优化主题系统，新增梦幻粉紫主题

### v1.1.0

- 🖼️ 随机图片生成器功能

### v1.0.0

- ✨ 初始版本发布
- 🔲 二维码生成和解码功能
- 🔗 URL参数编辑功能
- ⏰ 时间戳转换功能
- 📄 JSON工具（解析和比对）
- 🎨 渐变背景生成器
- 🔤 正则表达式测试工具

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

---

**享受开发，提高效率！** 🚀
